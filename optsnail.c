#include<stdio.h>
#include<stdlib.h>

int main()
{

  int size;
  int sum = 0;
  int x,y,i,j;
  int sw = 1;

  int ** snail;

  x = y = 0;

  printf("input of snail size: ");
  scanf("%d", &size);

  snail = (int **)malloc(size*sizeof(int));
  
  for(i=0; i<size; i++)
    {
      snail[
    }
